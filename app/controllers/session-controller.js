(function() {
    'use strict';

    define(['services/session-service'], SessionController);

    function SessionController(sessionService) {
        return {
            load: function() {

            }
        };
    }
})();